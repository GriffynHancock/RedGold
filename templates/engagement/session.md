# session.md — {{ENGAGEMENT_ID}}

Append-only working log. Archive closed sessions to `ledger/sessions/`.

---

## Session 001 — {{SCAFFOLD_DATE}} — Scaffold

**Did:** Engagement scaffolded by `/rg:new`. Boundary written to `scope.yaml` and validated.
Enforcement wired in `.claude/settings.json`.

**Authorisation on record:** {{SIGNED_BY}}, {{SIGNED_DATE}}, `{{AUTH_DOCUMENT}}`.
Window {{WINDOW_START}} to {{WINDOW_END}}. Mode `{{MODE}}`, ceiling {{CEILING}}.

**Next:** Gate 1 — the orchestrator writes a test plan naming, per phase, the assets to be touched,
the maximum tier, the classes of test, the endpoints expected to receive writes, and the expected
cleanup. The operator approves it once.

---
