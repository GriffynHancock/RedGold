# scripts/

Deterministic enforcement and validation (spec §9, §10).

## Interpreter — read this before wiring any hook

**These scripts require PyYAML, and the interpreter matters.** On the reference Kali VM:

| Interpreter | Version | PyYAML |
|---|---|---|
| `/usr/bin/python3` | 3.13 | **yes** (Debian `python3-yaml`) |
| `python3` on `PATH` (linuxbrew) | 3.14 | **no** |

A hook wired as bare `python3` therefore dies on import — and **a `PreToolUse` hook that crashes
does not deny, it fails open.** The enforcement layer written by `/rg:new` (step 4) must pin an
absolute interpreter path verified at scaffold time, never rely on `PATH` resolution.

`scope.require_yaml()` exists so this failure names itself instead of surfacing as a bare
traceback. Every guard must additionally wrap its own body and emit a **deny** decision on any
exception, so that a broken control is a closed control.

## Running the tests

```sh
/usr/bin/python3 -m unittest discover -s tests -v
```

stdlib `unittest`, deliberately — tests that gate a security control must run on a bare
interpreter without installing a test framework first.

## Files

Regenerated by `regen_scripts_readme.py` from what is actually on disk -- do not hand-edit the
table between the markers; it will be overwritten and drift is exactly what this file exists to
stop. Run `/usr/bin/python3 scripts/regen_scripts_readme.py` after adding, removing, or rewiring a
script, and `--check` to verify it hasn't drifted.

<!-- BEGIN GENERATED -->

| Script | Description | Wired as a hook |
|---|---|---|
| `baseline_scan.py` | the deterministic baseline (spec P10). | not a hook -- invoked directly (CLI / command backend) |
| `canary_check.py` | PreToolUse hook. Write authorisation (spec §9.4, §9.4.1). | `PreToolUse` |
| `findings.py` | the findings schema, evidence resolution, and validation rules (spec §10). | not a hook -- invoked directly (CLI / command backend) |
| `gate_cli.py` | the /rg:gate command (spec §9.3.2, §9.7). | not a hook -- invoked directly (CLI / command backend) |
| `new_engagement.py` | the /rg:new engagement scaffolder (spec §4, §7.1, §9.1, §15.1). | not a hook -- invoked directly (CLI / command backend) |
| `no_handrolled_loops.py` | PreToolUse hook. Denies hand-rolled request loops (spec §9.4). | `PreToolUse` |
| `no_nesting.py` | PreToolUse hook. A worker attempting to spawn a subagent is caught LOUDLY. | `PreToolUse` |
| `redact.py` | PostToolUse hook. Keeps credentials out of the transcript (spec §9.5). | `PostToolUse` |
| `regen_status.py` | regenerate status.md from the ledgers and findings (spec §7.2). | not a hook -- invoked directly (CLI / command backend) |
| `report.py` | the client deliverable (spec §10.6, §16). | not a hook -- invoked directly (CLI / command backend) |
| `scope.py` | scope.yaml — schema, parser, validator. | not a hook -- invoked directly (CLI / command backend) |
| `scope_cli.py` | the /rg:scope command (spec §5.2, §5.3, §5.4). | not a hook -- invoked directly (CLI / command backend) |
| `scope_guard.py` | PreToolUse hook. The core scope control (spec §9.3). | `PreToolUse` |
| `validate_agents.py` | CI check over the agent roster (spec §8, §9.1). | not a hook -- invoked directly (CLI / command backend) |
| `validate_findings.py` | SubagentStop hook. Findings validation (spec §9.6, §10). | `SubagentStop` |
| `verify_controls.py` | fault injection. Proves the tests discriminate. | not a hook -- invoked directly (CLI / command backend) |
| `rate_probe.sh` | the only sanctioned way to send a bounded burst (spec §9.4). | not a hook -- invoked directly (CLI / command backend) |

Generated from `scripts/*.py`, `scripts/*.sh` on disk and `HOOK_WIRING` in `new_engagement.py` -- run `regen_scripts_readme.py` after adding, removing, or rewiring a script. For what is spec'd but not yet built, see `status.md`'s "Remaining v1 gaps".

<!-- END GENERATED -->
